{
  "name": "dreamscape",
  "version": "1.0.0",
  "scripts": {
    "setup": "cd client && npm install && npm run build && cd ../server && npm install && npm run env",
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && npm run start"
  },
  "author": "",
  "license": "ISC"
}
