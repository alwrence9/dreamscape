image: node:18.18.0

stages:
  - lint
  - test

lint-client:
  stage: lint 
  script:
    - cd client && npm run lint
  allow_failure: false

  
lint-server:
  stage: lint 
  script:
    - cd server && npm run lint
  allow_failure: false

server-test:
  stage: test
  script:
    - cd server
    - npm install
    - npm run test
